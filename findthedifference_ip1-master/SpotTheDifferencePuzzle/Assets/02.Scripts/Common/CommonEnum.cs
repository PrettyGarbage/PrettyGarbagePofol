public enum BUILD_PHASE
{
    DEV, REAL
}

public enum PlatformType
{
    ANDROID = 0, IOS = 1
}

public enum LangType
{
    KR, JP, EN
}


public enum AudioType { BGM=0, FX, VOICE }

public enum ResourceID
{
    DETECTOR
}

public enum  AdType
{
    BANNER, INTERSTITIAL, INTERSTITIAL_VIDEO, REWARD
}

public enum AdRequestStatus
{
    NONE, LOADING, LOADED, FAIL, SHOW, CLOSED
}

public enum ResourcePoolType { }
public enum ResourceType { }


